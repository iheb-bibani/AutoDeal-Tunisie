# Copier ces deux valeurs depuis Supabase > Project Settings > API.
# Utiliser une publishable key (ou l'ancienne anon key), JAMAIS la secret/service-role key.
SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
SUPABASE_PUBLISHABLE_KEY = "sb_publishable_..."
